<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="org.example.entity.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="role" column="role" />
        <result property="name" column="name" />
        <result property="phone" column="phone" />
        <result property="email" column="email" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM user
    </select>

    <insert id="insert" parameterType="org.example.entity.User">
        INSERT INTO user (username, password, role, name, phone, email, create_time, update_time)
        VALUES (#{username}, #{password}, #{role}, #{name}, #{phone}, #{email}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="org.example.entity.User">
        UPDATE user
        SET username=#{username}, password=#{password}, role=#{role}, name=#{name}, phone=#{phone}, email=#{email}, create_time=#{createTime}, update_time=#{updateTime}
        WHERE id=#{id}
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM user WHERE id=#{id}
    </delete>
</mapper>
